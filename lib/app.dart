import 'package:flutter/material.dart';

class RandomCoffee extends StatelessWidget {
  const RandomCoffee({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}